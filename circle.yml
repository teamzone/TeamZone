machine:
  node:
    version: 4.3.2
    
general:
  artifacts:
    - "coverage.lcov" 
    
test:
  pre:
    - npm install -g grunt-cli
    
  override:
    - grunt devbuild 
# get a copy of build results to the test reports folder for sfae keeping and uniqueness    
    - cp test-results.xml $CIRCLE_TEST_REPORTS/devbuild-testResults.xml
 